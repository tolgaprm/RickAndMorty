<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comprmto.rickyandmorty">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".RickyAndMortyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RickyAndMorty">
        <activity
            android:name=".presentation.location.LocationActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.episode.EpisodeActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.character.CharecterActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.character.CharacterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>